apiVersion: v1
kind: Service
metadata:
  name: {{ include "ffc-helm-library.fullname" . }}
  labels:
    {{- include "ffc-helm-library.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ffc-helm-library.selectorLabels" . | nindent 4 }}
